[tool.poetry]
package-mode = false

[project]
name = "dependencies"
requires-python = ">=3.12,<3.13"
dependencies = [
    "coverage[toml]",
    "jsonschema",
    "pyfakefs",
    "pytest",
    "pytest-randomly",
]
