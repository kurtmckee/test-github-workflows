[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
coverage = { version = "*", extras = ["toml"] }
jsonschema = "*"
pytest = "*"
pytest-randomly = "*"
pyfakefs = "*"
